# Multiplayer

Clean multiplayer workspace for PexCraft networking work.

Current planned tree:

```text
multiplayer/
└── mcpe/
    └── protocol_81/
```

Keep protocol-specific join logic under the matching protocol folder. Do not use version-style names like `0154` here.
