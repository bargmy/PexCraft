name: Build all PexCraft targets and upload to Telegram

run-name: Build "${{ github.event.head_commit.message || github.ref_name }}"

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:

permissions:
  contents: read

env:
  CHAT_ID: '-1004462705182'

jobs:
  windows:
    name: Windows ${{ matrix.arch }} MinGW - ${{ github.event.head_commit.message || github.ref_name }}
    runs-on: windows-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86
            msystem: MINGW32
            gcc_package: mingw-w64-i686-gcc
            zlib_package: mingw-w64-i686-zlib
            out_exe: pexcraft-win-x86.exe
            remote_out_exe: pexcraft-remote-win-x86.exe
            artifact_prefix: pexcraft-windows-x86
            remote_artifact_prefix: pexcraft-rc-windows-x86
          - arch: x64
            msystem: MINGW64
            gcc_package: mingw-w64-x86_64-gcc
            zlib_package: mingw-w64-x86_64-zlib
            out_exe: pexcraft-win-x64.exe
            remote_out_exe: pexcraft-remote-win-x64.exe
            artifact_prefix: pexcraft-windows-x64
            remote_artifact_prefix: pexcraft-rc-windows-x64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read commit name
        id: commit
        shell: bash
        run: |
          name="$(git log -1 --pretty=%s)"
          safe="$(printf '%s' "$name" | tr -cs 'A-Za-z0-9._-' '-' | sed 's/^-//;s/-$//' | cut -c1-80)"
          [ -n "$safe" ] || safe="commit"
          echo "name=$name" >> "$GITHUB_OUTPUT"
          echo "safe=$safe" >> "$GITHUB_OUTPUT"

      - name: Set up MSYS2 MinGW
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          update: true
          install: >-
            base-devel
            ${{ matrix.gcc_package }}
            ${{ matrix.zlib_package }}
            zip

      - name: Build Windows clients
        shell: msys2 {0}
        run: |
          set +e
          mkdir -p build logs dist

          {
            echo "Commit name: ${{ steps.commit.outputs.name }}"
            echo "Commit SHA:  $GITHUB_SHA"
            echo "Builder:     Windows ${{ matrix.arch }} MinGW / ${{ matrix.msystem }}"
            echo
            gcc --version

            status=0

            gcc -std=c99 -O2 -Wall -Wextra -mwindows main.c -o build/${{ matrix.out_exe }} \
              -static -static-libgcc \
              -ld3d11 -ldxgi -ld3dcompiler -ld3d9 -lopengl32 -lglu32 \
              -lgdi32 -luser32 -lshell32 -lole32 -lwindowscodecs -lcomdlg32 -lwinmm -lws2_32 -lz -lm \
              || status=$?

            if [ "$status" -eq 0 ]; then
              gcc -std=c99 -O2 -Wall -Wextra -DPEX_REMOTE_HTTP=1 -DPEX_REMOTE_FPS=30 -mwindows main.c -o build/${{ matrix.remote_out_exe }} \
                -static -static-libgcc \
                -ld3d11 -ldxgi -ld3dcompiler -ld3d9 -lopengl32 -lglu32 \
                -lgdi32 -luser32 -lshell32 -lole32 -lwindowscodecs -lcomdlg32 -lwinmm -lws2_32 -lz -lm \
                || status=$?
            fi

            echo "$status" > build/build-status.txt

            if [ "$status" -eq 0 ]; then
              objdump -p build/${{ matrix.out_exe }} | sed -n 's/^[[:space:]]*DLL Name: //p' | sort -u > build/imported-dlls.txt || true
              echo "BUILD_OK=1" >> "$GITHUB_ENV"
            else
              echo "BUILD_OK=0" >> "$GITHUB_ENV"
            fi

            exit 0
          } 2>&1 | tee logs/build-windows-${{ matrix.arch }}.log

      - name: Package Windows normal zip
        if: env.BUILD_OK == '1'
        shell: pwsh
        run: |
          $commitName = '${{ steps.commit.outputs.name }}'
          $artifactPrefix = '${{ matrix.artifact_prefix }}'

          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $packageDir = Join-Path 'dist' $artifactPrefix
          New-Item -ItemType Directory -Force -Path $packageDir | Out-Null

          Copy-Item 'build/${{ matrix.out_exe }}' $packageDir -Force

          if (Test-Path 'assets') {
            Copy-Item 'assets' (Join-Path $packageDir 'assets') -Recurse -Force
          }

          if (Test-Path 'build/imported-dlls.txt') {
            Copy-Item 'build/imported-dlls.txt' $packageDir -Force
          }

          if (Test-Path 'logs') {
            Copy-Item 'logs' (Join-Path $packageDir 'logs') -Recurse -Force
          }

          @"
          Commit name: $commitName
          Commit SHA:  $env:GITHUB_SHA
          Builder:     Windows ${{ matrix.arch }} MinGW
          Includes:    normal Windows client only
          "@ | Out-File -FilePath (Join-Path $packageDir 'COMMIT_NAME.txt') -Encoding utf8

          $zipPath = Join-Path 'dist' "$artifactPrefix.zip"
          Compress-Archive -Path (Join-Path $packageDir '*') -DestinationPath $zipPath -Force
          "WIN_ZIP_PATH=$zipPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Package Opera 11 remote-client zip
        if: env.BUILD_OK == '1'
        shell: pwsh
        run: |
          $commitName = '${{ steps.commit.outputs.name }}'
          $artifactPrefix = '${{ matrix.remote_artifact_prefix }}'

          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $packageDir = Join-Path 'dist' $artifactPrefix
          New-Item -ItemType Directory -Force -Path $packageDir | Out-Null

          Copy-Item 'build/${{ matrix.remote_out_exe }}' $packageDir -Force

          if (Test-Path 'README_REMOTE_OPERA11.md') {
            Copy-Item 'README_REMOTE_OPERA11.md' $packageDir -Force
          }

          if (Test-Path 'assets') {
            Copy-Item 'assets' (Join-Path $packageDir 'assets') -Recurse -Force
          }

          if (Test-Path 'build/imported-dlls.txt') {
            Copy-Item 'build/imported-dlls.txt' $packageDir -Force
          }

          if (Test-Path 'logs') {
            Copy-Item 'logs' (Join-Path $packageDir 'logs') -Recurse -Force
          }

          @"
          Commit name: $commitName
          Commit SHA:  $env:GITHUB_SHA
          Builder:     Windows ${{ matrix.arch }} MinGW
          Includes:    Opera 11 remote HTTP client only
          Browser URL: http://WINDOWS_PC_IP:8425/
          "@ | Out-File -FilePath (Join-Path $packageDir 'COMMIT_NAME.txt') -Encoding utf8

          $zipPath = Join-Path 'dist' "$artifactPrefix.zip"
          Compress-Archive -Path (Join-Path $packageDir '*') -DestinationPath $zipPath -Force
          "RC_ZIP_PATH=$zipPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Upload Windows normal artifact
        if: env.BUILD_OK == '1'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_prefix }}
          path: ${{ env.WIN_ZIP_PATH }}
          if-no-files-found: warn

      - name: Upload Opera 11 remote-client artifact
        if: env.BUILD_OK == '1'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.remote_artifact_prefix }}
          path: ${{ env.RC_ZIP_PATH }}
          if-no-files-found: warn

      - name: Mark failed build in summary
        if: env.BUILD_OK != '1'
        shell: bash
        run: |
          echo "### Windows ${{ matrix.arch }} normal/remote build failed" >> "$GITHUB_STEP_SUMMARY"
          echo "Commit name: \`${{ steps.commit.outputs.name }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "No artifact uploaded for this builder." >> "$GITHUB_STEP_SUMMARY"

  linux:
    name: Linux SDL2/OpenGL - ${{ github.event.head_commit.message || github.ref_name }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read commit name
        id: commit
        shell: bash
        run: |
          name="$(git log -1 --pretty=%s)"
          safe="$(printf '%s' "$name" | tr -cs 'A-Za-z0-9._-' '-' | sed 's/^-//;s/-$//' | cut -c1-80)"
          [ -n "$safe" ] || safe="commit"
          echo "name=$name" >> "$GITHUB_OUTPUT"
          echo "safe=$safe" >> "$GITHUB_OUTPUT"

      - name: Install Linux dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libsdl2-dev \
            libsdl2-image-dev \
            libcurl4-openssl-dev \
            zlib1g-dev \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            zip

      - name: Build Linux SDL2/OpenGL
        shell: bash
        run: |
          set +e
          mkdir -p build logs dist

          {
            echo "Commit name: ${{ steps.commit.outputs.name }}"
            echo "Commit SHA:  $GITHUB_SHA"
            echo "Builder:     Linux SDL2/OpenGL"
            echo
            gcc --version
            make -f Makefile.linux_sdl2

            status=$?
            echo "$status" > build/build-status.txt

            if [ "$status" -eq 0 ]; then
              echo "BUILD_OK=1" >> "$GITHUB_ENV"
            else
              echo "BUILD_OK=0" >> "$GITHUB_ENV"
            fi

            exit 0
          } 2>&1 | tee logs/build-linux-sdl2.log

      - name: Package Linux zip
        if: env.BUILD_OK == '1'
        shell: bash
        run: |
          set -euxo pipefail
          artifact_prefix="pexcraft-linux-x64"
          package_dir="dist/$artifact_prefix"

          mkdir -p "$package_dir"

          find . -maxdepth 4 -type f \( -name 'pexcraft_sdl2' -o -name 'pexcraft' -o -name 'pexcraft-linux*' \) -exec cp -f {} "$package_dir/" \; || true

          cp -R assets "$package_dir/assets" || true
          cp -R logs "$package_dir/logs" || true

          cat > "$package_dir/COMMIT_NAME.txt" <<EOF
          Commit name: ${{ steps.commit.outputs.name }}
          Commit SHA:  $GITHUB_SHA
          Builder:     Linux SDL2/OpenGL
          EOF

          zip_path="dist/$artifact_prefix.zip"
          (cd "$package_dir" && zip -r "../$(basename "$zip_path")" .)
          echo "ZIP_PATH=$zip_path" >> "$GITHUB_ENV"

      - name: Upload Linux artifact
        if: env.BUILD_OK == '1'
        uses: actions/upload-artifact@v4
        with:
          name: pexcraft-linux-x64
          path: ${{ env.ZIP_PATH }}
          if-no-files-found: warn

      - name: Mark failed build in summary
        if: env.BUILD_OK != '1'
        shell: bash
        run: |
          echo "### Linux SDL2/OpenGL failed" >> "$GITHUB_STEP_SUMMARY"
          echo "Commit name: \`${{ steps.commit.outputs.name }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "No artifact uploaded for this builder." >> "$GITHUB_STEP_SUMMARY"

  psp:
    name: PSP - ${{ github.event.head_commit.message || github.ref_name }}
    runs-on: ubuntu-latest
    container:
      image: pspdev/pspdev:latest

    steps:
      - name: Install checkout dependencies inside PSP container
        shell: bash
        run: |
          apk add --no-cache git nodejs npm zip make python3 || true
          apt-get update && apt-get install -y git nodejs npm zip make python3 || true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Read commit name
        id: commit
        shell: bash
        run: |
          name="$(git log -1 --pretty=%s)"
          safe="$(printf '%s' "$name" | tr -cs 'A-Za-z0-9._-' '-' | sed 's/^-//;s/-$//' | cut -c1-80)"
          [ -n "$safe" ] || safe="commit"
          echo "name=$name" >> "$GITHUB_OUTPUT"
          echo "safe=$safe" >> "$GITHUB_OUTPUT"

      - name: Build PSP
        shell: bash
        run: |
          set +e
          mkdir -p build logs dist

          {
            echo "Commit name: ${{ steps.commit.outputs.name }}"
            echo "Commit SHA:  $GITHUB_SHA"
            echo "Builder:     PSP"
            echo
            make -f Makefile.psp

            status=$?
            echo "$status" > build/build-status.txt

            if [ "$status" -eq 0 ]; then
              echo "BUILD_OK=1" >> "$GITHUB_ENV"
            else
              echo "BUILD_OK=0" >> "$GITHUB_ENV"
            fi

            exit 0
          } 2>&1 | tee logs/build-psp.log

      - name: Package PSP zip
        if: env.BUILD_OK == '1'
        shell: bash
        run: |
          set -euxo pipefail
          artifact_prefix="pexcraft-psp-mips"
          package_dir="dist/$artifact_prefix"

          mkdir -p "$package_dir"

          find . -maxdepth 5 -type f \( \
            -name 'EBOOT.PBP' -o \
            -name 'PARAM.SFO' -o \
            -name '*.elf' -o \
            -name '*.prx' -o \
            -name '*.pbp' \
          \) -exec cp -f {} "$package_dir/" \; || true

          cp -R assets "$package_dir/assets" || true
          cp -R logs "$package_dir/logs" || true

          cat > "$package_dir/COMMIT_NAME.txt" <<EOF
          Commit name: ${{ steps.commit.outputs.name }}
          Commit SHA:  $GITHUB_SHA
          Builder:     PSP
          EOF

          zip_path="dist/$artifact_prefix.zip"
          (cd "$package_dir" && zip -r "../$(basename "$zip_path")" .)
          echo "ZIP_PATH=$zip_path" >> "$GITHUB_ENV"

      - name: Upload PSP artifact
        if: env.BUILD_OK == '1'
        uses: actions/upload-artifact@v4
        with:
          name: pexcraft-psp-mips
          path: ${{ env.ZIP_PATH }}
          if-no-files-found: warn

      - name: Mark failed build in summary
        if: env.BUILD_OK != '1'
        shell: bash
        run: |
          echo "### PSP failed" >> "$GITHUB_STEP_SUMMARY"
          echo "Commit name: \`${{ steps.commit.outputs.name }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "No artifact uploaded for this builder." >> "$GITHUB_STEP_SUMMARY"

  telegram:
    name: Upload successful builds to Telegram
    runs-on: ubuntu-latest
    needs:
      - windows
      - linux
      - psp
    if: always() && github.event_name != 'pull_request'

    steps:
      - name: Read commit name
        id: commit
        shell: bash
        run: |
          name="${{ github.event.head_commit.message || github.ref_name }}"
          safe="$(printf '%s' "$name" | tr -cs 'A-Za-z0-9._-' '-' | sed 's/^-//;s/-$//' | cut -c1-80)"
          [ -n "$safe" ] || safe="commit"
          echo "name=$name" >> "$GITHUB_OUTPUT"
          echo "safe=$safe" >> "$GITHUB_OUTPUT"

      - name: Download successful artifacts
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          path: telegram_upload

      - name: Upload successful build zips to Telegram
        env:
          TOKEN: ${{ secrets.TOKEN }}
          CHAT_ID: ${{ env.CHAT_ID }}
          COMMIT_NAME: ${{ steps.commit.outputs.name }}
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${TOKEN:-}" ]; then
            echo "Missing Telegram bot token. Add repository secret TOKEN." >&2
            exit 1
          fi

          python3 - <<'PY'
          import os, sys, glob, json, uuid, mimetypes, urllib.request, urllib.parse

          token = os.environ["TOKEN"]
          chat_id = os.environ["CHAT_ID"]
          commit_name = os.environ.get("COMMIT_NAME", "")
          repo = os.environ.get("GITHUB_REPOSITORY", "")
          sha = os.environ.get("GITHUB_SHA", "")

          files = sorted(glob.glob("telegram_upload/**/*.zip", recursive=True))

          caption = (
              "PEXCraft successful builds\n"
              f"Commit name: {commit_name}\n"
              f"Repo: {repo}\n"
              f"Commit: {sha[:12]}\n"
              f"Built artifacts: {len(files)}"
          )

          def send_text(text):
              data = urllib.parse.urlencode({
                  "chat_id": chat_id,
                  "text": text,
              }).encode()
              req = urllib.request.Request(f"https://api.telegram.org/bot{token}/sendMessage", data=data)
              with urllib.request.urlopen(req, timeout=120) as r:
                  print(r.read().decode("utf-8", "replace"))

          if not files:
              send_text(caption + "\nNo successful build artifacts were produced.")
              raise SystemExit(0)

          def send_group(group, group_index):
              boundary = "----pexcraft-" + uuid.uuid4().hex
              parts = []
              media = []

              for i, path in enumerate(group):
                  attach_name = f"file_{group_index}_{i}"
                  entry = {
                      "type": "document",
                      "media": f"attach://{attach_name}",
                  }
                  if i == 0:
                      entry["caption"] = caption
                  media.append(entry)

              def field(name, value):
                  parts.append(
                      (
                          f"--{boundary}\r\n"
                          f"Content-Disposition: form-data; name=\"{name}\"\r\n\r\n"
                          f"{value}\r\n"
                      ).encode()
                  )

              def filefield(name, path):
                  filename = os.path.basename(path)
                  ctype = mimetypes.guess_type(filename)[0] or "application/zip"
                  parts.append(
                      (
                          f"--{boundary}\r\n"
                          f"Content-Disposition: form-data; name=\"{name}\"; filename=\"{filename}\"\r\n"
                          f"Content-Type: {ctype}\r\n\r\n"
                      ).encode()
                  )
                  with open(path, "rb") as f:
                      parts.append(f.read())
                  parts.append(b"\r\n")

              field("chat_id", chat_id)
              field("media", json.dumps(media))

              for i, path in enumerate(group):
                  filefield(f"file_{group_index}_{i}", path)

              parts.append(f"--{boundary}--\r\n".encode())

              req = urllib.request.Request(
                  f"https://api.telegram.org/bot{token}/sendMediaGroup",
                  data=b"".join(parts),
                  headers={"Content-Type": f"multipart/form-data; boundary={boundary}"},
              )

              with urllib.request.urlopen(req, timeout=240) as r:
                  print(r.read().decode("utf-8", "replace"))

          for index in range(0, len(files), 10):
              send_group(files[index:index + 10], index // 10)
          PY